name: PulseTracker Tests

on: [push, pull_request]

jobs:
   build:
       name: Checkout master
       runs-on: ubuntu-18.04
       steps:
       - uses: actions/checkout@v2
       - name: Install
         run: make install && make
       - name: Run Unit Test
         run: sh install.sh && make lint 
